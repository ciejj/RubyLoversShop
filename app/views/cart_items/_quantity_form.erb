<%= form_for(cart_item, url: cart_item_path(cart_item)) do |f| %>
  <div class='form-group'>
    <%= f.select :quantity, [*0..9], {}, { value: cart_item.quantity, class: 'form-control cart_item-update-quantity', onchange: 'this.form.submit();' } %>
  </div>
<% end %>
