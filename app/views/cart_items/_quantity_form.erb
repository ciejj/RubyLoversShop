<%= form_for(cart_item, url: cart_item_path(cart_item)) do |f| %>
  <div class='form-group'>
    <%= f.select :quantity, [*1..9], {}, { value: cart_item.quantity, class: 'form-control', onchange: 'this.form.submit();' } %>
  </div>
<% end %>