<h3 class="my-4">Categories</h4>
<div class="list-group">
  <% Category.all.each do |category|%>
    <%= link_to category.name, root_path(category: category.id), class: "list-group-item" %>
  <% end %>
</div>

